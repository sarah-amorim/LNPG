# Dada a lista letras = ["a", "b", "c", "d", "e", "f"], obtenha uma nova lista com os elementos do índice 1 até o 3 (inclusive o 1, exclusivo o 4).

# Com a lista numeros = [10, 20, 30, 40, 50], use slicing para obter os três primeiros elementos.

# A partir da lista valores = [1, 2, 3, 4, 5, 6], crie uma nova lista contendo os elementos a partir do índice 3.

# Dada a lista pares = [0, 2, 4, 6, 8, 10, 12], crie uma nova lista pegando um elemento a cada dois.

# Dada a lista nomes = ["Ana", "Bea", "Caio", "Duda"], use slicing para criar uma nova lista invertida.
